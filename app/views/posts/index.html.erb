<div class="container gray-contact blog-index">
  <div class="row">
    <% flash.each do |type, msg| %>
      <%= content_tag :div, msg, class: "alert alert-#{type}" %>
    <% end %>
    
    <div class="col-md-3">
      <%= image_tag 'picture.jpg', class: 'blogPic text-center' %>
      <h1>kyle grondin</h1>
    </div>
    <div class="col-md-9">
      <% @posts.each do |post| %>
        <div class="blog-post">
          <h2>
          <%= link_to post_date_path(post.year, post.month, post.slug) do %>
            <%= post.title %>
          <% end %>
          </h2>
          <p class="blog-date"><%= localize(post.created_at, :format => :long) %></p>
          <p><%= markdown (post.content) %></p>
          <hr class="blog-hr">
        </div>
      <% end %>
      
      <%= link_to "new post", new_post_path, class: 'btn btn-default btn-lg' %>
      

      <%= will_paginate @posts %>
 
    </div>
  </div>
</div>